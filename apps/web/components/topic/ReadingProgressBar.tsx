/**
 * ⚡ ReadingProgressBar — Pure CSS scroll-driven progress indicator
 * Zero JavaScript. Uses animation-timeline: scroll()
 */

export default function ReadingProgressBar() {
  return <div className="reading-progress" aria-hidden="true" />;
}
